
const initialState = {
    locations: [],
    searchActive:false
}
export const reducer = (state = initialState,action) => {
    return state;
} 

export const setSearch = (state, action) => {
    
}